[package]
name = "davimci-app"
description = "Frontend-agnostic view state"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[features]
# Golden view-state fixtures, shared with the frontends' rendering tests.
testing = []

[dependencies]
davimci-core = { workspace = true, features = ["testing"] }
davimci-cmd.workspace = true
davimci-keys.workspace = true
davimci-motion.workspace = true
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
